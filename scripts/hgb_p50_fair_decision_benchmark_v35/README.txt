HGB vs TinyML-MLP P50-QAT fair class-decision CPU benchmark v3.5
Run from the TinyML_IDS_IEEE project root.
Both locked seed-2026 models are measured in five isolated processes.
Timed output semantics are class labels: TinyML argmax(logits), HGB predict().
Validation/test data are not accessed and model-family selection is not reopened.
